type Props = {
  hidden: boolean,
  content: {
    modal?: string,
    card?: string,
    cta?: string,
    color?: string,
    image?: string
  }
}

export const Modal = ({ content, hidden }: Props) => {

  return <div className="min-w-[44rem] bg-white ">


  </div>
}